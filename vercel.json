{
  "buildCommand": "flutter build web --release",
  "outputDirectory": "build/web",
  "devCommand": "flutter run -d web-server --web-port 3000",
  "installCommand": "if [ ! -d \"$HOME/.flutter\" ]; then git clone https://github.com/flutter/flutter.git $HOME/.flutter; fi && $HOME/.flutter/bin/flutter --version && $HOME/.flutter/bin/flutter pub get",
  "framework": null,
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/",
      "headers": {
        "cache-control": "public, max-age=0, must-revalidate"
      }
    }
  ]
}
